# Title

## Pre-watch questions

## Chunk 1
(only write concepts)


## Chunk 1 converted
- the `3 main points`
- the `plain English explanation`
- `why it matters`
- `one example`

### 3 main points
1. We can manage nulls in spark by setting them as either a value from one column of value from another
2. we can also just set a default value
3. Most null functionality comes from na filed that has advanced fill method with many overloads


### Plain English
We can deal with nulls in spark by using values from other columns as default values or by having static default values.

na field from data frames has many functionalities to deal with nulls mostly in its fill method.

 we can additionally just remove any rows that have nulls

we can check for nulls with isnull method
## Chunk 1 compressed
Concept: ...
Why it matters: ...
Example: ...
One confusion: ...
